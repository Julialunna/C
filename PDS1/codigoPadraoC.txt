#include <stdio.h>
#define SUCESSO 0
int main(int argc, char*argv[]){
    return SUCESSO;
}

// uso de arquivo 
 
    if(arquivoEntrada==NULL){
        //printf("Erro na abertura do arquivo");
        exit(1);
    }